{
  "name": "infinite-todo",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "node scripts/generate-deployment-timestamp.mjs dev",
    "dev": "wrangler dev",
    "predeploy": "node scripts/generate-deployment-timestamp.mjs deploy",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
